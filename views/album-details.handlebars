<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>{{albumName}}</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <h1>Photos in {{albumName}} ({{count}} {{#if (eq count 1)}}photo{{else}}photos{{/if}})</h1>
    <ul>
      {{#each photos}}
        <li><a href="/photo-details/{{id}}">{{title}}</a></li>
      {{/each}}
    </ul>
    <p><a href="/">← Back to albums</a></p>
  </div>
</body>
</html>
