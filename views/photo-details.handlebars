<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Photo Details</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <h1>Photo Details</h1>
    <p><a href="/edit-photo?pid={{photo.id}}">Edit</a></p>

    <div class="photo">
      <h3>{{photo.title}}</h3>
      <p><strong>Description:</strong> {{photo.description}}</p>
      <img src="/photos/{{photo.filename}}" alt="{{photo.title}}">
    </div>

    <p><a href="/">← Back to albums</a></p>
  </div>
</body>
</html>
